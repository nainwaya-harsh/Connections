import 'package:flutter/material.dart';

class EventGuest extends StatelessWidget {
  const EventGuest({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}